# handoff-091-epk

## Where we left off

**This turn (Sep 9, 2026, Opus 5):** ARRIRAW confirmed, the AFI contact named, the two competing documents collapsed to one, spacing opened up, and the readout rebuilt with a hatching rule. **`press/epk.json` rev 19 · 38 of 47 = 81%.**

### What I could not do, and why it is said plainly

Luke asked for the content written **into the Google Doc**, with this lane's `.docx` deleted and the Doc made primary.

**Writing into a `.gdoc` is not possible from this machine.** Checked before answering rather than assumed: no Google Docs connector installed, no `googleapiclient`, no `gdrive`/`gam`/`gcloud`. A `.gdoc` on disk is a 175-byte pointer whose own contents say any change made to it will be lost.

What was done instead gets him to the same place in two clicks:

1. The document is renamed **`EPK INFO — all the text.docx`** — the same name as the intended primary, so that **Drive → right-click → Open with → Google Docs** produces a Doc already correctly titled.
2. The old `EPK INFO (Claude).docx` is **deleted**, as asked.
3. What remains is the stale `.gdoc` and one current `.docx`. After the convert, both can go.

Deleting the `.docx` before the Doc existed would have left only the stale copy, which is the failure this whole thread started with.

### Corrections

**`3.11` is Alexa 35 ARRIRAW.** Luke dictated "RE RAW" twice; it was recorded as dictated with the uncertainty flagged in the field's history and a query printed on the PDF. He has now corrected it himself. The instinct to store what he said rather than what seemed right was correct procedure, and the flag is what surfaced it.

**`3.17` was mislabelled.** It is not a press contact — it is the **AFI contact**, matching the rights block on Joan's kit: `AFI CONSERVATORY / CONTACT PERSON / EMAIL`. Relabelled **"AFI contact (name and email)"**, filled with **Susan Dretzka · sdretzka@afi.com**. Stored in normal case; the EPK page applies its own capitals, so the caps in his message are presentation rather than content. Section 3 is now 18 of 19.

### The hatching rule

Luke asked for section 4, the director's statement, to carry the slashed fill as well. It is **computed, not hardcoded**: a field whose text also appears as a candidate in some other field's undecided choice is drawn hatched. `4.1`'s statement is one of the two candidates in `2.2`, so until the synopsis is settled the statement's use is unsettled too — and the bar now says so.

The legend changed with it: **settled · written, use not settled · not supplied**. The count is 38 supplied, 3 unsettled, 7 outstanding.

---

**Timestamp:** 2026-09-09
**Lane:** `epk`
**Continues:** handoff-090-epk.md
**Model:** Opus 5.

## Next action (the one thing)

**Luke: open `EPK INFO — all the text.docx` with Google Docs, then delete the old `.gdoc` and the `.docx`.** That leaves one primary document, and this lane rebuilds the `.docx` beside it whenever the data changes. Then Jordan on `2.1` and `2.2` — the only section still at zero, and settling `2.2` also settles the statement. Unanswered since handoff-062: decimal `3.15` or Roman `III.15`.

## Files

- The list: https://scroggdawg.github.io/kom-festival-board/epk.html
- Data: https://github.com/Scroggdawg/kom-festival-board/blob/main/press/epk.json
- PDF: https://github.com/Scroggdawg/kom-festival-board/blob/main/press/EPK-breakdown.pdf
- On the Drive: `05 MARKETING/00 PRESS/EPK INFO — all the text.docx`
- Prior handoff: https://github.com/Scroggdawg/kom-festival-board/blob/main/handoffs/handoff-090-epk.md
- This handoff: https://github.com/Scroggdawg/kom-festival-board/blob/main/handoffs/handoff-091-epk.md

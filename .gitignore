.DS_Store
*.swp
.vercel
__pycache__/

# Illustrator sources live on the Drive, not here: the three .ai files are 70 MB
# together and this repo publishes GitHub Pages, which caps a published site at 1 GB.
# Rebuild them with tools/build-credit-card-ai.py — they are generated, not authored.
press/illustrator/
# build-epk-ai.py's generated ExtendScript and log; the .ai it makes lives on the Drive
tools/.epk-ai-build/

# Redesign scratch: contains rendered pages of ANOTHER film's press kit (the NightFeeds
# reference) and builder experiments. Not ours to republish; never committed.
press/credit-redesign/
